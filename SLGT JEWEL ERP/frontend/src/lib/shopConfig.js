// Central jewellery-shop business configuration.
//
// The shop's WhatsApp/contact number is editable in Settings → Company Profile
// (stored as `whatsapp_number` on the free-form `/settings/company` record) —
// that value always takes precedence. This constant is only the fallback used
// until a shop owner sets one, so the number is never hardcoded per-component.
export const DEFAULT_SHOP_WHATSAPP_NUMBER = "+91 76590 71470";
