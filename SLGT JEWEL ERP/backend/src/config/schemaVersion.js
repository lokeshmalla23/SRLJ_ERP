/**
 * Application schema version for cloud ↔ branch compatibility checks (future sync).
 * Bump when migrations introduce breaking structural changes.
 */
export const SCHEMA_VERSION = 14;


export const SCHEMA_VERSION_KEY = 'schema_version';
